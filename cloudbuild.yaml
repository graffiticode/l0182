steps:
  # Build Docker image (the Dockerfile installs deps via `npm ci` on node:22-alpine,
  # matching the lockfile — no redundant top-level install on a mismatched node:latest).
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/graffiticode/l0182:$COMMIT_SHA"
      - "."

  # Push image to Container Registry
  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - "gcr.io/graffiticode/l0182:$COMMIT_SHA"
    id: Push

  # Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk:slim"
    args:
      - run
      - deploy
      - l0182
      - "--platform=managed"
      - "--image=gcr.io/graffiticode/l0182:$COMMIT_SHA"
      - >-
        --labels=commit-sha=$COMMIT_SHA,gcb-build-id=$BUILD_ID,gcb-trigger-id=$_TRIGGER_ID
      - "--region=$_DEPLOY_REGION"
      - "--quiet"
      # --update-env-vars, NOT --set-env-vars: set replaces the whole ENVIRONMENT, so any
      # variable added out of band is silently deleted by the next deploy. update leaves the
      # rest alone. Keep this rule in any new deploy path.
      - "--update-env-vars=AUTH_URL=$_AUTH_URL"
      # Set explicitly, and NOT merely omitted. Cloud Run carries the service's current
      # scaling forward when the flag is absent, and this service was pinned to
      # --max-instances=1 while it ran an in-memory mock survey backend. That backend is gone
      # — nothing here holds state between requests any more — but dropping the flag would
      # leave the old pin of 1 in place indefinitely. Siblings run at 20.
      - "--max-instances=$_MAX_INSTANCES"
      - "--allow-unauthenticated"
    id: Deploy
    entrypoint: gcloud

images:
  - "gcr.io/graffiticode/l0182:$COMMIT_SHA"

options:
  substitutionOption: ALLOW_LOOSE

substitutions:
  _MAX_INSTANCES: "20"
  _DEPLOY_REGION: us-central1
  _AUTH_URL: https://auth.graffiticode.org

tags:
  - l0182
